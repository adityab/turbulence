`LINK-CLUSTERSPACE(NODE(C))`
===========================

::
    Q = queue of clusterspace roots

    ...TK
    IS-A relationships
    For X in Q
        Success = COMPARE-CLASSES(N, X)
        If Success
            break

    Associations
    TODO
            
